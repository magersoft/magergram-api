type User {
  fullName: String
  postsCount: Int!
  followingCount: Int!
  followersCount: Int!
  newNotificationsCount: Int!
  isFollowing: Boolean!
  isSelf: Boolean!
  isRequestingSubscription: Boolean!
}
